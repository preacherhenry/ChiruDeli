/** Mirrors SERVICE_FEE in apps/api/src/modules/orders/orders.service.ts — shown
 * here only as an estimate; the server always computes and charges the real total. */
export const ESTIMATED_SERVICE_FEE = 5;
